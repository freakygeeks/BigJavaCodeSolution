
//  interface Sortable -- makes an array of objects sortable by providing
//                        a function that can compare them

public interface Sortable {
    public int compare(Sortable other);
}

